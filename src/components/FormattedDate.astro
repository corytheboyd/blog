---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time
  datetime={date.toISOString()}
  class:list={[
    "font-mono text-xs px-2 py-0.5 mr-2",
    "bg-gray-200 text-black",
    "dark:bg-black dark:text-gray-300",
  ]}
>
  {date.toISOString().split("T")[0].trim()}
</time>
